pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello from first pipeline'
                sh 'echo "Hello again"'
                sh 'date'
            }
        }
    }

}